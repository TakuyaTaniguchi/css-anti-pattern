# css-anti-pattern

## subTitle

**main** -- bad code

``` CSS
h2 {
    foo: bar;
}
```

**main** -- good code

``` CSS
h1, h2, h3 {
    foo: bar;
}
```

## subTitle

**main** -- bad code

``` CSS
h2 {
    foo: bar;
}
```

**main** -- good code

``` CSS
h1, h2, h3 {
    foo: bar;
}
```